{
  "name": "9.taak_grid_generator",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --open --host",
    "build": "tsc && vite build",
    "preview": "vite preview --open --host",
    "deploy": "surge ./dist jvh-grid-generator.surge.sh",
    "postdeploy": "start brave https://jvh-grid-generator.surge.sh"
  },
  "devDependencies": {
    "@types/randomcolor": "^0.5.9",
    "typescript": "~5.6.2",
    "vite": "^6.0.5"
  },
  "dependencies": {
    "randomcolor": "^0.6.2"
  }
}
